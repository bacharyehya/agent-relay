name: AgentRelay
options:
  bundleIdPrefix: com.bash
packages:
  AgentRelayPackage:
    path: .
targets:
  AgentRelayMac:
    type: application
    platform: macOS
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    sources:
      - App/MacApp
    dependencies:
      - package: AgentRelayPackage
        product: AppCore
