# AgentRelay

Native macOS-first, local-first AI collaboration workspace with a background core service, SQLite storage, and a separate MCP adapter.

## Verification

Run the package test suite:

```bash
swift test
```

Run the end-to-end handoff smoke check:

```bash
./Scripts/smoke_handoff.sh
```
